{
    "name": "js-info-tutorial",
    "version": "1.0.0",
    "description": "",
    "main": "server.js",
    "scripts": {
        "start": "npx nodemon --exec npx gulp -e * --ignore app",
        "serve": "npx http-server",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "express": "^4.17.1",
        "gulp": "^4.0.2",
        "http-server": "^0.12.3",
        "nodemon": "^2.0.6"
    },
    "dependencies": {
        "socket.io": "^3.0.0"
    }
}
